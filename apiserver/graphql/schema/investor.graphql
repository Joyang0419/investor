type Investor {
    investorID: ID!
    name: String!
    orders: [Order!]!
}

extend type Query {
    investors: [Investor!]!
}

input createInvestorInput {
    name: String!
}

extend type Mutation {
    createInvestor(input: createInvestorInput!): Investor!
}

