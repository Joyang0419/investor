type Order {
    orderID: ID!
    investorID: ID!
    amount: Float!
}

extend type Query {
    orders: [Order!]!
}

input createOrderInput {
    investorID: ID!
    amount: Float!
}

extend type Mutation {
    createOrder(input: createOrderInput!): Order!
}