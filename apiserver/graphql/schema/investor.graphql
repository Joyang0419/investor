# TODO 寫一下 interface 的用途
interface BaseResponse {
    code: Int!
    message: String!
}

## TODO 下面之後, 先寫個範例的概念
type Query {
    echo(s: String!): QueryEchoOutput!
}

type QueryEchoOutput implements BaseResponse{
    code: Int!
    message: String!
    Data: String!
}


type Mutation {
    echo(s: String!): MutationEchoOutput!
}
type MutationEchoOutput implements BaseResponse{
    code: Int!
    message: String!
    Data: String!
}
